{
  "name": "guess_it",
  "version": "1.3.1",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "refresh": "node db/refresh.js",
    "pm2": "pm2 delete guess_it && pm2 start npm --name \"guess_it\" -- start",
    "pm2:refresh": "pm2 delete guess_it_refresh && pm2 start npm --name \"guess_it_refresh\" -- run refresh"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "wtf_wikipedia": "^10.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
